1. ContactDetailsDB
	Restore DB backup file in sql server.

2. EHI_Assignment

	Under one solution it has four differnt projects consisting DataAccess, Interfaces, Implementation of interfaces and 		.net core as a start up which contains controller and views.
	
	Need to do only one change i.e, 
		go to starup project => find appsettings.json => and change the server to your DB provider name to run the 		app.
